// Detox 20 uses globalSetup/globalTeardown automatically
// No adapter code needed - Jest environment handles Detox lifecycle
// Custom setup per test suite can go here if needed

jest.setTimeout(120000);
